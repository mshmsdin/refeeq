# تعليمات تطوير مشروع رفيق

## قاعدة التوثيق والنشر

- ملف `client/public/llms.txt` هو المرجع العام لفهم الموقع؛ يجب تحديثه مع كل ميزة جديدة أو تغيير في آلية العمل أو مسار أو واجهة برمجية أو إعداد نشر.
- إذا غيّر التطوير سلوكاً يراه المستخدم، فحدّث صفحة التعليمات `/rafeeq/f1` في نفس التغيير، مع شرح مكان الميزة وطريقة استخدامها.
- عند إضافة مسار عام أو تغيير اسمه أو حذفه، حدّث `client/public/sitemap.xml` واحذف أي رابط أصبح قديماً.
- عند تغيير نطاق الفهرسة أو إضافة مسارات داخلية، حدّث `client/public/robots.txt` بحيث تبقى الصفحات العامة قابلة للفهرسة وتبقى الواجهات البرمجية والمزامنة والفحص الداخلي غير مفهرسة.
- لأن التطبيق يعمل تحت المسار الأساسي `/rafeeq/`، يجب أن تستخدم الروابط العامة هذا المسار، وألا تفترض أن الجذر هو مسار التطبيق.

## فحص إلزامي قبل التسليم

- نفّذ `npm.cmd --prefix client run build` بعد أي تغيير في الواجهة أو الملفات العامة.
- نفّذ `node --check server/server.js` بعد أي تغيير في الخادم.
- نفّذ `git diff --check` للتأكد من عدم وجود أخطاء تنسيق.
- تأكد من أن الملفات العامة الأربعة موجودة داخل `client/public/` حتى تدخل صورة الإنتاج التي ينشئها فيت.
- لا تضف ملفات البيئة السرية أو قواعد البيانات أو الوسائط أو مجلدات الاعتماديات أو ناتج البناء إلى المستودع.
- قبل الرفع راجع حالة جت هب والفرق الفعلي، ثم اذكر بوضوح ما تم محلياً وما تم رفعه، ولا تعتبر الرفع نشرًا حياً إلا بعد فحص الموقع المنشور.

## مبدأ الحفاظ على الدقة

- لا توثّق ميزة أو مساراً غير موجود في الكود.
- عند تعديل مسار أو واجهة برمجية، ابحث في `llms.txt` و`sitemap.xml` و`robots.txt` وصفحة `/f1` وعدّل كل موضع متأثر في التغيير نفسه.
- حافظ على اتجاه الواجهة العربية من اليمين إلى اليسار، وعلى أسماء الأقسام والتصنيفات الظاهرة للمستخدم.
